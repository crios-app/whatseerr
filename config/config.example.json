{
  "//_comment": "Copy this file to config.json and fill in your Jellyseerr + WAHA details.",
  "system": {
    "protocol": "http",
    "host": "localhost",
    "logging": {
      "level": "info",
      "timestamps": false
    }
  },
  "services": {
    "jellyseerr": {
      "port": 5055,
      "apiKey": "YOUR_JELLYSEERR_API_KEY",
      "defaultUserId": 2
    },
    "waha": {
      "port": 8584,
      "apiKey": "YOUR_WAHA_API_KEY",
      "session": "default"
    }
  },
  "webhook": {
    "requests": {
      "path": "/requests",
      "port": 3006
    },
    "seerr": {
      "path": "/seerr"
    },
    "//_library_comment": "Library webhook for Plex / Sonarr / Radarr. When set up, users following a movie or show receive a WhatsApp DM the moment a new episode or movie hits the library. Set 'token' to require a ?token=... query string for authentication (recommended when exposing this endpoint).",
    "library": {
      "path": "/library",
      "token": ""
    }
  },
  "mappings": {
    "//_userIdMappings_comment": "Map WAHA phone numbers to Jellyseerr user IDs. Use phone number without @c.us suffix as key. Each entry must have 'userId' (number) and 'username' (string) properties. LID format (@lid) is automatically resolved to phone number for lookup. Add 'patreonEmail' (string) to link this WhatsApp number to a Patreon account for tier gating; if omitted, the bot falls back to the email it knows from Seerr (emailMappings).",
    "userIdMappings": {
      "1234567890": {
        "userId": 1,
        "username": "",
        "patreonEmail": ""
      }
    },
    "//_emailMappings_comment": "Map email addresses to Jellyseerr user IDs. Automatically populated when webhook notifications are received.",
    "emailMappings": {},
    "//_lidMappings_comment": "Map phone number format chatIds to LID format chatIds. This allows using LID format (more private) when sending messages. Format: \"phoneNumber@c.us\": \"lidNumber@lid\". Mappings are automatically created when LID format messages are received.",
    "lidMappings": {}
  },
  "commands": {
    "command": "r,request,s,search",
    "command4k": "r4k,request4k",
    "help4k": false,
    "//_follow_comment": "Comma-separated aliases for the follow command (subscribe to a title without creating a Seerr request).",
    "followCommand": "follow,f",
    "//_unfollow_comment": "Comma-separated aliases for the unfollow command.",
    "unfollowCommand": "unfollow,uf",
    "//_calendar_comment": "Comma-separated aliases for the calendar command (shows upcoming episodes / releases for followed media).",
    "calendarCommand": "calendar,cal"
  },
  "//_patreon_comment": "Restricts the bot to Premium / Diamond Patreon supporters. Leave 'accessToken' empty to disable gating entirely. 'help' and 'link' always bypass; admin users bypass too. Generate a Creator's Access Token + OAuth client at https://www.patreon.com/portal/registration/register-clients. 'campaignId' is the numeric ID of your Patreon campaign. 'allowedTiers' is a case-insensitive list of tier titles that grant access. 'upgradeUrl' is shown in the rejection message. 'refreshIntervalMinutes' controls patron-list re-fetch cadence. The OAuth fields (clientId/clientSecret/redirectUri) enable the self-service `link` command: patrons send `link` to receive a one-time Patreon authorization URL; once they authorize, their phone is linked to their Patreon account automatically. The redirectUri must point at this bot's `/patreon/callback` and must be whitelisted in your Patreon API client's 'Redirect URIs' field.",
  "patreon": {
    "accessToken": "",
    "campaignId": "",
    "allowedTiers": ["Premium", "Diamond"],
    "upgradeUrl": "",
    "refreshIntervalMinutes": 30,
    "clientId": "",
    "clientSecret": "",
    "redirectUri": ""
  },
  "cache": {
    "searchResultsTTL": 3600,
    "pendingSelectionsTTL": 1800,
    "processedMessagesTTL": 86400,
    "promptTimestampsTTL": 3600
  },
  "queues": {
    "messageConcurrency": 5,
    "apiConcurrency": 3,
    "webhookConcurrency": 10
  },
  "rateLimit": {
    "maxRequests": 100,
    "timeWindow": "1 minute"
  }
}